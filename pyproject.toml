[project]
name = "card-game-simulator"
version = "0.1.0"
description = "A Python simulation of the Blackjack card game,where players draw cards and try to beat the dealer without going over 21."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
